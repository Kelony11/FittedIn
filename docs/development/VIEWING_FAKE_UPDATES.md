# 查看假用户更新指南

## 如何让真实用户看到假用户的内容

### 重要：连接真实用户到假用户

为了让真实用户在Feed中看到假用户的内容，需要先建立连接。运行以下脚本：

```bash
cd backend
npm run connect:real-to-fake
```

这会自动为所有真实用户创建与假用户的连接（默认每个真实用户连接10个假用户）。

### 步骤 1: 运行内容生成脚本

```bash
cd backend
npm run generate:content
```

这会生成：
- 新帖子 (5-15个)
- 目标进度更新
- 帖子点赞和评论
- 连接请求

### 步骤 2: 刷新前端页面

在浏览器中：
1. 登录到 FittedIn（使用你的真实用户账号）
2. 进入 Dashboard 页面
3. **刷新页面**（按 F5 或 Cmd+R）
4. 向下滚动查看 Activity Feed - 现在应该能看到假用户的帖子了！

**注意**：Feed只显示已连接用户的内容。如果你看不到内容，确保已经运行了 `npm run connect:real-to-fake`

### 步骤 3: 查看不同页面的更新

**Dashboard 页面** (`dashboard.html`):
- Activity Feed 区域会显示新的帖子和活动
- 右上角的通知bell图标会显示新通知（如果有）
- 点击通知可以查看详细信息

**Goals 页面** (`goals.html`):
- 目标进度会显示更新
- 已完成的目标会显示完成状态

**Connections 页面** (`connections.html`):
- 新的连接请求会出现在这里

### 检查内容是否生成

如果想确认内容是否真的生成了，可以：

1. **查看数据库**:
```bash
docker-compose exec postgres psql -U postgres -d fittedin_dev -c "SELECT id, user_id, content, created_at FROM posts ORDER BY created_at DESC LIMIT 10;"
```

2. **检查最近生成的帖子**:
```bash
docker-compose exec postgres psql -U postgres -d fittedin_dev -c "SELECT p.id, u.display_name, p.content, p.created_at FROM posts p JOIN users u ON p.user_id = u.id WHERE u.email LIKE '%@fittedin-seeded.com' ORDER BY p.created_at DESC LIMIT 5;"
```

### 故障排除

**问题：看不到新帖子**

解决方案：
1. 确保你已经运行了 `npm run generate:content`
2. 确保你登录的用户与其他假用户有连接（accepted connections）
3. Feed只显示你连接的用户的帖子
4. 刷新页面（Ctrl+F5 强制刷新）
5. 检查浏览器控制台是否有错误

**问题：时间戳显示为刚刚生成**

这是因为内容生成时时间戳设置为最近1-24小时内。这是正常的，可以让内容看起来是最近发布的。

**问题：想要更多内容**

可以运行多次：
```bash
npm run generate:content
npm run generate:content
npm run generate:content
```

或者设置环境变量生成更多：
```bash
MAX_CONTENT_PER_RUN=30 npm run generate:content
```

### 自动生成

如果想自动生成内容（每小时一次），可以使用PM2：

```bash
cd backend
npm run pm2:start
```

这会自动每小时生成新的内容。

### 查看日志

生成脚本的输出会显示：
- 生成了什么类型的内容
- 哪个用户创建了内容
- 生成摘要

### 示例输出

```
✅ Dynamic content generation completed!

📊 Summary:
   Posts: 5
   Goal Updates: 2
   Post Interactions: 3
   Connection Requests: 1
   Total: 11
```

这表示成功生成了11个内容项！

